%header.page-header#header
  = render 'partials/navigation'

  -# the logo, link home
  .page-header-text
    %h1
      = link_to "/" do
        -#= image_tag 'hero/IoR_logo_text.svg', alt: 'Isle of Ruby 2018'
        Heart of Clojure Activities
    %h2 Events during and around the conference
